# This is here since we are using a custom yaml file for the GitHub pages Jekyll build. 
# If anything breaks in the future, check if the bottom link is working because that's where the Jekyll gem (Ruby library)
# is being pulled from. If that link is broken, then the build will fail, so first thing to do is to check and maybe update it.
source 'https://rubygems.org'
gem 'jekyll'